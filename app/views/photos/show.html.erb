<p id="notice"><%= notice %></p>

<h1><%= @photo.name %></h1>
<p>
  <%= image_tag @photo.image.url(:original), size: "400x300"%>
</p>
<h2>
  <strong>By:</strong>
  <%= @photo.user.name %>
</h2>
 <h3>Add a comment:</h3>
 <%= render 'comments/form' %>

<%= link_to 'Back', photos_path %>
